// Configuración de producción. No importar este archivo directamente:
// siempre importar '.../environments/environment' y dejar que el build lo reemplace.
export const environment = {
  production: true,
  apiUrl: 'https://api.tiendabox.com/api',
};
